# Optional: docker-compose for local dev (simple service running vite)
version: '3.8'
services:
  web:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - 5173:5173
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
