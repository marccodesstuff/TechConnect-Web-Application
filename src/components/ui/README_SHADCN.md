Shadcn notes:

- After running `npx shadcn-ui@latest init`, the generator will add files to this folder and update `tailwind.config.cjs`.
- Keep generated components here and export them from an index if needed.
- The current `Button` is a small hand-made example to demonstrate `class-variance-authority` patterns.
